# 决策日志摘要

技术栈统一采用 Vue 3 + TypeScript + Vite + Vant 4 + Pinia + Vue Router + Axios，保留移动端优先的 SPA MVP 方案，删除 SSR、微前端、A/B 平台、自研监控、复杂权限矩阵等非必要设计。Top5 修复点：统一 P0/P1/P2 边界；明确登录拦截与游客购物车策略；补齐 API 超时、错误码、重试与 Loading 规则；完善表单校验和极端状态；将排期收敛为 16-20 人天可交付范围。

# 终版完整规格书

## 一、产品需求 PRD

### 1.1 用户角色

**Persona A：价格敏感型种草用户，18-24 岁**  
关注低价、满减、优惠券、真实评价和促销活动，主要使用移动端碎片化浏览。

**Persona B：品质保障型成熟用户，25-35 岁**  
关注正品保障、成分安全、物流时效、复购便利性和售后服务。

**Persona C：礼品/临时采购用户**  
关注礼盒包装、加急配送、收货时效和下单效率。MVP 不单独建设礼品模式，仅在商品标签、包装信息和备注能力中覆盖。

### 1.2 功能优先级

**P0 / MVP 必须交付**

- 首页：Banner、分类入口、推荐商品流。
- 分类与商品列表：分类浏览、基础排序、分页加载。
- 搜索：关键词搜索、搜索历史、无结果状态。
- 商品详情：图片轮播、价格、促销标签、SKU 选择、库存展示、评价摘要、图文详情。
- 购物车：加入购物车、修改数量、删除、选中、全选、合计金额。
- 结算：地址选择、商品清单、运费展示、优惠金额展示、提交订单。
- 支付结果：支付跳转、回跳结果展示、重新支付入口。
- 登录注册：手机号 + 验证码登录，协议勾选。
- 个人中心：用户信息、订单入口、地址入口、退出登录。
- 订单：订单列表、订单详情、基础状态操作。
- 基础体验：Loading、空状态、错误状态、Toast、接口失败重试。

**P1 / 第二阶段**

- 地址管理：新增、编辑、删除、默认地址。
- 优惠券：领券中心、我的优惠券、结算页选择优惠券。
- 收藏商品。
- 发布评价：星级、文字、图片上传。
- 正品验证查询。
- 售后申请：退款、退货退款、凭证上传、进度展示。
- 搜索筛选：价格区间、品牌、销量、新品。

**P2 / 远期**

- 个性化推荐。
- 直播购物、短视频种草。
- AR 试妆。
- 社区 UGC。
- 第三方客服 SDK。
- 多语言、多币种。

### 1.3 核心业务流程

**浏览-加购-结算-支付**  
首页/分类/搜索进入商品详情，用户选择 SKU 后加入购物车。购物车支持勾选商品并进入结算页。结算页校验登录态、收货地址和库存状态，提交订单后调用支付接口跳转第三方收银台。支付回跳后进入支付结果页，前端按订单号查询支付状态并展示结果。

**登录注册**  
用户进入受保护页面或点击“我的”时进入登录页。输入手机号、勾选协议、获取验证码，验证码按钮进入 60 秒倒计时。登录成功后写入 token 和用户基础信息，并跳回 redirect 指定页面。

**搜索与筛选**  
用户输入关键词进入搜索结果页，搜索词写入历史记录。筛选和排序状态写入路由 query，返回列表页时保持原筛选条件和滚动位置。

**售后申请**  
用户从订单详情进入售后页，选择售后类型，填写原因，上传凭证图片，提交后进入售后进度页。前端仅展示后端返回的售后状态。

### 1.4 页面路由

```text
/                          首页
/category                  分类页
/category/:id              分类商品列表
/search                    搜索结果页
/product/:id               商品详情页
/cart                      购物车
/checkout                  结算页
/pay/result                支付结果页
/login                     登录注册
/user                      个人中心
/user/orders               订单列表
/user/orders/:id           订单详情
/user/orders/:id/aftersale 售后申请/进度
/user/address              地址管理
/user/address/edit         地址新增/编辑
/user/favorites            我的收藏
/user/coupons              我的优惠券
/coupon/center             领券中心
/review/publish/:orderId   发布评价
/verify                    正品验证
/404                       兜底页
```

受保护路由包括 `/checkout`、`/user/*`、`/review/*`、售后相关页面。未登录访问时跳转 `/login?redirect=当前路径`。

## 二、前端技术架构

### 2.1 技术栈

- 框架：Vue 3 + TypeScript。
- 构建：Vite。
- UI 组件：Vant 4。
- 状态管理：Pinia。
- 路由：Vue Router 4。
- HTTP：Axios。
- 样式：Sass + CSS Variables。
- 适配：viewport/rem 二选一，项目内统一。
- 规范：ESLint + Prettier + Husky + lint-staged。
- Mock：MSW 或 Vite Mock，采用一种即可。

### 2.2 目录结构

```text
src/
  api/
    request.ts
    product.ts
    cart.ts
    order.ts
    user.ts
    coupon.ts
  assets/
  components/
    common/
    business/
  views/
    Home/
    Category/
    Product/
    Cart/
    Checkout/
    User/
    Order/
  store/
    user.ts
    cart.ts
    app.ts
  router/
    index.ts
    guards.ts
  hooks/
  utils/
  types/
  mock/
  styles/
```

### 2.3 状态管理

**Pinia 全局状态**

- `userStore`：token、用户信息、登录状态。
- `cartStore`：购物车商品、选中状态、数量、合计金额。
- `appStore`：全局 Loading 计数、网络状态、Toast 队列。
- `addressStore`：地址列表、默认地址。

**组件局部状态**

- 表单输入值。
- 弹窗、抽屉、ActionSheet 开关。
- 当前页面筛选项、排序项、分页游标。
- 当前页面 Loading、Error、Empty 状态。

购物车支持游客加购，本地持久化到 localStorage；登录后调用合并购物车接口，以后端结果覆盖本地状态。

### 2.4 路由设计

页面组件统一懒加载。路由守卫读取 `meta.requiresAuth`，未登录时跳转登录页。列表页通过路由 query 保存筛选、排序、关键词；返回时恢复滚动位置。404 使用通配路由兜底。

## 三、UI/UX 设计规范

### 3.1 布局

移动端优先。一级页面使用顶部搜索/标题栏 + 内容区 + 底部 TabBar，TabBar 包含首页、分类、购物车、我的。详情页、结算页、支付页不展示 TabBar，使用顶部返回栏和底部固定操作栏。

弹层统一使用底部 Popup 或 ActionSheet，适用于 SKU、优惠券、地址选择等场景。

### 3.2 视觉规范

- 风格：清新、精致、可信赖，避免强促销感。
- 主色：珊瑚粉 `#FF6F91`。
- 背景：奶油白 `#FFF8F5`。
- 正文：深灰 `#333333`。
- 辅助文字：浅灰 `#999999`。
- 强调：金色 `#D4A056`，用于正品、会员、保障标签。
- 状态色：成功 `#52C41A`，警告 `#FAAD14`，错误 `#F5222D`。
- 字体：系统字体栈 `-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif`。
- 字号：标题 18-20px，正文 14-15px，辅助文字 12px。
- 间距：8px 栅格。
- 圆角：卡片 8-12px，按钮按 Vant 默认规范调整。

### 3.3 页面说明

首页展示搜索框、Banner、分类入口、活动楼层和推荐商品流，支持下拉刷新和上拉加载。

分类页展示一级分类和商品网格，切换分类刷新商品列表。

搜索页展示搜索框、历史词、热词、筛选栏和商品列表，筛选项同步到 query。

商品详情页展示图片、价格、促销、SKU、库存、正品保障、评价摘要和图文详情。底部固定“加入购物车”“立即购买”。

购物车页展示商品列表、数量步进器、删除、全选、合计金额和结算按钮。空购物车展示返回首页入口。

结算页展示地址、商品清单、优惠券、运费、金额明细和提交订单按钮。无地址时引导新增地址。

支付结果页展示成功、失败、处理中三种状态，并提供查看订单、重新支付、返回首页。

登录页展示手机号、验证码、协议勾选和登录按钮，未勾选协议禁止提交。

个人中心展示用户信息、订单状态入口、收藏、优惠券、地址、正品验证、设置。

订单列表按全部、待付款、待发货、待收货、待评价分 Tab 展示。订单详情根据状态展示取消、支付、确认收货、评价、售后等按钮。

地址页支持新增、编辑、删除、设为默认。删除前弹出确认。

售后页支持售后类型、原因、说明、图片凭证和进度时间线。

评价页支持星级、文字、图片和匿名开关。

正品验证页输入防伪码，展示后端返回的验证结果。

## 四、前端交互与逻辑

### 4.1 交互规则

所有异步提交按钮进入 Loading 状态并防重复点击。所有成功、失败、库存不足、登录失效、网络超时均用明确 Toast 或页面错误状态反馈。

加购、提交订单、支付查询、删除地址、取消订单等操作必须防抖或锁定按钮。

删除、取消订单、退出登录等不可逆操作必须二次确认。

### 4.2 表单校验

登录表单：手机号必填且符合 11 位中国大陆手机号格式；验证码为 4-6 位数字；协议必须勾选。

地址表单：联系人、手机号、省市区、详细地址必填；详细地址 5-120 字；手机号格式合法。

结算表单：必须存在有效地址，必须存在可购买商品，库存不足商品禁止提交。

评价表单：星级必选，文字最多 500 字，图片最多 6 张。

售后表单：售后类型、原因必填；说明最多 300 字；图片最多 6 张。

### 4.3 数据模型

```ts
type Product = {
  id: string
  name: string
  mainImage: string
  images: string[]
  price: number
  originalPrice?: number
  sales: number
  stock: number
  tags: string[]
  skuList: Sku[]
}

type Sku = {
  id: string
  productId: string
  specs: Record<string, string>
  price: number
  stock: number
  image?: string
}

type CartItem = {
  id: string
  productId: string
  skuId: string
  count: number
  checked: boolean
  product: Product
  sku: Sku
}

type Order = {
  id: string
  status: 'pending_pay' | 'paid' | 'shipped' | 'received' | 'completed' | 'closed'
  totalAmount: number
  freightAmount: number
  discountAmount: number
  address: Address
  items: CartItem[]
  createdAt: string
}
```

### 4.4 状态处理

首屏接口展示骨架屏。局部操作展示按钮 Loading。列表无数据展示空状态。接口失败展示错误状态和重试按钮。网络断开时保留页面内容并提示网络异常。登录过期时清空 token 并跳转登录页。

## 五、API 调用方案

### 5.1 Axios 封装

`request.ts` 创建统一 Axios 实例，配置 `baseURL`、`timeout=10000`、请求头和响应拦截器。

请求拦截器自动注入 `Authorization: Bearer <token>`。

响应拦截器统一处理：

- 2xx 且业务 code 成功：返回业务 data。
- 401：清空登录态并跳转登录页。
- 403：提示无访问权限。
- 404：进入业务空状态或兜底页。
- 5xx：提示服务异常。
- timeout：提示网络超时并允许重试。
- 业务错误码：展示后端 message。

### 5.2 API 清单

```text
POST /api/v1/auth/send-code
POST /api/v1/auth/login
GET  /api/v1/products
GET  /api/v1/products/:id
GET  /api/v1/categories
GET  /api/v1/cart
POST /api/v1/cart/items
PUT  /api/v1/cart/items/:id
DELETE /api/v1/cart/items/:id
POST /api/v1/cart/merge
GET  /api/v1/addresses
POST /api/v1/addresses
PUT  /api/v1/addresses/:id
DELETE /api/v1/addresses/:id
POST /api/v1/orders
GET  /api/v1/orders
GET  /api/v1/orders/:id
POST /api/v1/orders/:id/pay
GET  /api/v1/orders/:id/pay-status
GET  /api/v1/coupons
POST /api/v1/coupons/:id/receive
POST /api/v1/reviews
POST /api/v1/aftersales
GET  /api/v1/verify
```

### 5.3 Mock 规则

Mock 数据覆盖首页、商品、购物车、订单、地址、优惠券、登录成功、登录失败、库存不足、网络异常。Mock 响应结构必须与后端约定一致。

## 六、非功能需求

### 6.1 性能

- 首屏 LCP 控制在 2.5s 内。
- 商品图片使用 WebP 或等效压缩格式。
- 列表图片懒加载。
- 路由级代码分割。
- 长列表使用分页加载，单页渲染数量控制在 20-30 条。
- 打包产物启用 gzip/brotli 压缩。

### 6.2 兼容性

支持 iOS Safari 13+、Android Chrome 80+、微信内置浏览器。点击热区不小于 44x44px。适配 360px-430px 主流移动端宽度，并兼容基础桌面浏览。

### 6.3 可维护性

接口类型集中定义，页面不直接拼接接口地址。金额统一以分为单位存储，展示时格式化为元。时间统一使用 ISO 字符串，展示层格式化。环境变量区分开发、测试、生产。组件拆分以复用和清晰为准，不建设复杂跨应用架构。

### 6.4 异常场景

覆盖无商品、无搜索结果、空购物车、无地址、优惠券不可用、库存不足、支付处理中、支付失败、订单关闭、图片上传失败、网络断开、登录过期、接口超时。

## 七、开发排期与里程碑

### 7.1 排期

**阶段一：项目搭建与基础能力，2 人天**  
完成 Vite、Vue、TypeScript、Vant、Pinia、Router、Axios、样式变量、Mock、代码规范。

**阶段二：首页、分类、搜索、商品详情，5 人天**  
完成商品浏览链路、SKU 选择、商品列表分页、搜索与基础筛选。

**阶段三：购物车、结算、支付结果，4 人天**  
完成购物车状态、游客购物车、结算校验、提交订单、支付结果展示。

**阶段四：登录、个人中心、订单、地址，5 人天**  
完成登录注册、路由守卫、个人中心、订单列表、订单详情、地址管理。

**阶段五：联调、测试与优化，4 人天**  
完成真实 API 联调、错误态补齐、移动端适配、性能优化、Bug 修复。

总计：20 人天，单名前端约 4 周，两名前端约 2 周。

### 7.2 里程碑

- T+2：项目可运行，基础架构和 Mock 完成。
- T+7：商品浏览链路完成。
- T+11：购物车、结算、支付结果完成。
- T+16：用户、订单、地址完成。
- T+20：联调测试完成，具备 MVP 上线条件。
